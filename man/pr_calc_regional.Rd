% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-regional.R
\name{pr_calc_regional}
\alias{pr_calc_regional}
\title{Compute Parameters by Region}
\usage{
pr_calc_regional(
  trial,
  masks = NULL,
  parameters = c("mpp", "mvp", "max_force", "contact_area", "pti_mean"),
  threshold = 0
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{masks}{Named list of \link{pr_mask} objects or logical matrices. If
\code{NULL} (default), uses the layout's default regions.}

\item{parameters}{Character vector. Parameters to compute. Supported
values: \code{"mpp"}, \code{"mvp"}, \code{"max_force"}, \code{"mean_force"},
\code{"contact_area"}, \code{"pti_max"}, \code{"pti_mean"}.
Default \code{c("mpp", "mvp", "max_force", "contact_area", "pti_mean")}.}

\item{threshold}{Numeric. Default \code{0}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one column \code{region} plus one column
per requested parameter.
}
\description{
Applies region masks to a trial and computes summary parameters
independently for each region. Returns one row per region.
}
\examples{
trial <- pr_example_trial("saddle_horse")
pr_calc_regional(trial)
}
