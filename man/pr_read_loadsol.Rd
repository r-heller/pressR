% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-loadsol.R
\name{pr_read_loadsol}
\alias{pr_read_loadsol}
\title{Read loadsol Force Data}
\usage{
pr_read_loadsol(path, force_cols = NULL, time_col = "time", verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to the CSV file.}

\item{force_cols}{Character vector. Names of force columns. If \code{NULL},
all numeric columns other than the first (time) column are used.}

\item{time_col}{Character. Name of the time column. Default \code{"time"}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A \link{pr_trial} object with a minimal single-row layout
(one sensor per force zone).
}
\description{
Parses a CSV exported from novel's loadsol app. The loadsol system
records a small number of force zones (not a full pressure matrix);
this parser returns a simplified \link{pr_trial} where each "sensor" is a
force zone. Pressure values are reported in Newtons.
}
\examples{
tmp <- tempfile(fileext = ".csv")
df <- data.frame(time = seq(0, 1, by = 0.01),
                 heel = runif(101, 0, 200),
                 mid  = runif(101, 0, 150),
                 fore = runif(101, 0, 300))
utils::write.csv(df, tmp, row.names = FALSE)
trial <- pr_read_loadsol(tmp, verbose = FALSE)
trial$n_sensors
}
