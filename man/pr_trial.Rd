% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-trial.R
\name{pr_trial}
\alias{pr_trial}
\title{Create a Pressure Trial Object}
\usage{
pr_trial(pressure, time, layout, metadata = list(), sampling_hz = NULL)
}
\arguments{
\item{pressure}{Numeric matrix of dimensions \verb{n_frames x n_sensors}.
Each row is one time frame, each column is one active sensor.}

\item{time}{Numeric vector of length \code{n_frames}. Timestamps in seconds
from recording start.}

\item{layout}{A \link{pr_layout} object defining the sensor arrangement.}

\item{metadata}{Named list of trial metadata. Expected (optional) fields:
\code{subject_id}, \code{trial_id}, \code{date}, \code{condition}, \code{system}, \code{notes}.}

\item{sampling_hz}{Numeric. Sampling rate in Hz. If \code{NULL}, computed
from \code{time}.}
}
\value{
A \code{pr_trial} S3 object.
}
\description{
Represents a single pressure measurement recording: a time series of
pressure frames measured across a sensor layout.
}
\examples{
trial <- pr_example_trial("pedar")
print(trial)
}
