% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-layout.R
\name{pr_layout}
\alias{pr_layout}
\title{Create a Pressure Sensor Layout}
\usage{
pr_layout(
  grid_rows,
  grid_cols,
  active,
  coords_mm,
  regions = list(),
  sensor_area_cm2 = 1,
  pressure_range = c(0, 600),
  pressure_unit = "kPa",
  name = "custom",
  description = "",
  manufacturer = "",
  model = ""
)
}
\arguments{
\item{grid_rows}{Integer. Number of rows in the sensor grid.}

\item{grid_cols}{Integer. Number of columns in the sensor grid.}

\item{active}{Logical matrix of dimensions \verb{grid_rows x grid_cols}.
\code{TRUE} for cells that contain active sensors.}

\item{coords_mm}{Data frame with columns \code{sensor_id} (integer),
\code{row} (integer), \code{col} (integer), \code{x_mm} (numeric), \code{y_mm} (numeric).
Physical coordinates of each active sensor in millimeters.}

\item{regions}{Named list of logical matrices (same dims as \code{active}).
Each entry defines an anatomical or functional region.}

\item{sensor_area_cm2}{Numeric. Area of a single sensor cell in cm².}

\item{pressure_range}{Numeric vector of length 2. Min and max measurable
pressure in kPa.}

\item{pressure_unit}{Character. Unit of pressure measurement. Default \code{"kPa"}.}

\item{name}{Character. Short identifier for this layout
(e.g., \code{"insole_standard"}).}

\item{description}{Character. Human-readable description.}

\item{manufacturer}{Character. Sensor manufacturer. Default \code{""}.}

\item{model}{Character. Sensor model/system name.}
}
\value{
A \code{pr_layout} S3 object (list with class attribute).
}
\description{
Defines the physical arrangement of sensors in a pressure measurement
system, including grid dimensions, active sensor positions, physical
coordinates, and named region masks.
}
\examples{
active <- matrix(TRUE, 4, 4)
coords <- data.frame(
  sensor_id = seq_len(16),
  row = rep(1:4, each = 4),
  col = rep(1:4, times = 4),
  x_mm = rep(seq(0, 30, 10), times = 4),
  y_mm = rep(seq(0, 30, 10), each = 4)
)
layout <- pr_layout(4, 4, active, coords, name = "example_4x4")
print(layout)
}
