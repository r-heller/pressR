% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-heatmap.R
\name{pr_plot_heatmap}
\alias{pr_plot_heatmap}
\title{Plot Pressure Heatmap}
\usage{
pr_plot_heatmap(
  trial,
  frame = NULL,
  type = c("mpp", "mvp", "pti", "contact"),
  show_regions = FALSE,
  palette = "viridis",
  range = NULL,
  interpolate = FALSE,
  title = NULL
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{frame}{Integer. Frame number to display. If \code{NULL} (default),
shows the summary picture specified by \code{type}.}

\item{type}{Character. Summary type when \code{frame} is \code{NULL}:
\code{"mpp"} (max), \code{"mvp"} (mean), \code{"pti"} (pressure-time integral),
\code{"contact"} (contact frequency).}

\item{show_regions}{Logical. Overlay region boundaries. Default \code{FALSE}.}

\item{palette}{Character. One of \code{"viridis"} (default), \code{"inferno"},
\code{"plasma"}, \code{"magma"}, \code{"jet"}, \code{"novel"}.}

\item{range}{Numeric vector of length 2. Colour range. \code{NULL} auto-scales.}

\item{interpolate}{Logical. Reserved for spatial interpolation (currently
draws the raw sensor grid). Default \code{FALSE}.}

\item{title}{Character. Plot title. \code{NULL} auto-generates.}
}
\value{
A \code{ggplot2} object.
}
\description{
Displays a 2D heatmap for a single frame or a summary image
(MPP, MVP, PTI, contact frequency).
}
\examples{
trial <- pr_example_trial("pedar")
pr_plot_heatmap(trial)
}
