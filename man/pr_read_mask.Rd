% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-mask.R
\name{pr_read_mask}
\alias{pr_read_mask}
\title{Read novel Mask File}
\usage{
pr_read_mask(path, layout = NULL, name = "imported", verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to the mask file.}

\item{layout}{A \link{pr_layout} object. Required if the mask dimensions
must be validated against a specific layout; otherwise, the mask is
returned as-is.}

\item{name}{Character. Name for the resulting \link{pr_mask} object.
Default \code{"imported"}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A \link{pr_mask} object (if a layout is supplied) or a logical
matrix.
}
\description{
Parses \code{.msa} / \code{.msr} / \code{.msp} mask files exported from the novel
software. The expected format is a text matrix of 0/1 (or non-zero)
values, optionally preceded by a short header.
}
\examples{
tmp <- tempfile(fileext = ".msa")
writeLines(c(
  "1 1 0 0",
  "1 1 0 0",
  "0 0 1 1",
  "0 0 1 1"
), tmp)
m <- pr_read_mask(tmp, verbose = FALSE)
dim(m)
}
